"""Bundled Graphic Walker + React assets served by :func:`polars_gw.walk`.

Built from the ``js/`` directory with ``npm run build``.  Contents:

- ``graphic-walker.js`` — minified IIFE that exposes ``window.__gwpRender``.
- ``graphic-walker.css`` — Tailwind-compiled stylesheet.
- ``versions.json`` — pinned npm versions + build timestamp.
"""
